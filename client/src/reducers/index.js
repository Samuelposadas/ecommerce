/* eslint-disable */
const initialState = {
  product: {},
  allProducts: [],
  copyAllProducts: [],
  allCategories: [],
  filterProducts: [],
};

export const reducer = (store = initialState, action) => {
  switch (action.type) {
    default:
      return store;
  }
};
