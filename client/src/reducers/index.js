const initialState = {
  product: {},
  allProdcuts: [],
  copyAllProdcuts: [],
  allCategories: [],
  filterProducts: [],
};

export const reducer = (store = initialState, action) => {
  switch (action.type) {
    default:
      return store;
  }
};
