const initialState = {
  product: {},
  allProdcuts: [],
  copyAllProducts: [],
  allCategories: [],
  filterProducts: [],
};

export const reducer = (store = initialState, action) => {
  switch (action.type) {
    default:
      return store;
  }
};
